exposed a = {};

a.one = 2;
a.two = 1;

i = 0;

while i < 9 {
    a[i] = i;
    i = i + 1;
}

applied = |value| {
    value = value - 1;
    print(value);
};

for_each(a, applied);

applied2 = |key val| {
    val = 0;
    print(key, val);
};

for_each_kv(a, applied2);
